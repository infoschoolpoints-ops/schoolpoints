# רשימת מטלות - SchoolPoints 📋

## מטלות שהושלמו לאחרונה ✅

### יציבות תחנות ו‑DB (פברואר 2026) ✅
- [x] מניעת כתיבת `db_path` לקובץ config משותף כדי שלא להפיץ נתיב מקומי לעמדות אחרות.
- [x] חיזוק זיהוי מחשב מקומי (שם/FQDN/IP) והמרת UNC/IP לנתיב מקומי למניעת שגיאות הרשאה כשכמה עמדות רצות על אותו מחשב.
- [x] המתנה וריטריי מתמשכים בעת נעילות DB (busy/locked) כדי למנוע הודעות כשל בפתיחה.
- [x] Bootstrap אוטומטי לעמדת Local Sync (משיכת snapshot מהשרת המקומי/העתקה מ-UNC) כדי למנוע מסד נתונים ריק בתחנה חדשה.
- [x] הוספת שדות “תפקיד עמדה” ו“מצב מערכת” בחלון הגדרות ראשוניות ושמירתם לקונפיג.
- [x] מצב חירום: מעבר אוטומטי ל‑DB מקומי כאשר ה‑DB המשותף נפתח לקריאה בלבד.
- [x] עמדת ניהול יכולה להפעיל שרת Local Sync כעמדה ראשית (כמו העמדה הציבורית).
- [x] תפקיד עמדה/מצב מערכת מוצגים כסטטוס מזוהה בלבד (ללא שינוי ידני).
- [x] מניעת כפילות שרת Local Sync ע"י בדיקת /sync/status לפני הפעלה.
- [x] עדכון גרסה ל‑1.6.1 בכל העמדות והקובץ generator.
- [x] הגדלת חלון ההגדרות הראשוניות כדי למנוע צפיפות/גודל קטן.
- [x] ניסיון הסרת Read‑Only והמרת UNC לפני מעבר חירום כדי למנוע false‑positive.
- [x] הוספת ריטריי/בדיקת כתיבה ל‑DB לפני מעבר חירום, כולל BEGIN IMMEDIATE ובדיקת הרשאות.
- [x] צמצום נעילות DB ע"י החלת PRAGMA journal_mode פעם אחת לכל DB (cache) והגדרת synchronous לכל חיבור.
- [x] מניעת תקיעה בהפעלת עמדת ניהול: הגבלת זמן המתנה בעת נעילת DB (create_tables) ודילוג אם DB קיים.

### תיקוני סנכרון ענן (פברואר 2026) ✅
- [x] תיקון אירועי סנכרון כפולים (duplicate key) ב-`sync_events` באמצעות INSERT אידמפוטנטי.
- [x] יישור `app_monolith.py`: שימוש ב-`local_id` ל-event_id והכנסת `sync_events` אידמפוטנטית (ON CONFLICT/OR IGNORE).

### תיקוני התחברות והרשמה (פברואר 2026) ✅
- [x] תיקון זרימת כניסת מוסד/מורה כולל `/web/signin` ו-`/web/logout`.
- [x] שדרוג אימות סיסמה לתמיכה ב-hash ישנים.
- [x] החלפת דוגמת שם מוסד אמיתי בעמוד ההרשמה.

### אופטימיזציות ביצועים (ינואר 2026) ✅
- [x] צמצום לוגים ואתחולים מיותרים בעמדה הציבורית (לוגים דיבוג לפי דגל בלבד)
- [x] אופטימיזציה לסריקות צלילים (marker לקאש + cache לרשימת צלילים)
- [x] אתחול עצל ל-SoundManager בעמדת ניהול
- [x] חיבור עצל/אסינכרוני למסך לקוח בעמדת קופה

### חדשות מתוזמנות (Scheduled News) ✅
- [x] הוספת שדות `start_date` ו-`end_date` לטבלת `news_items`
- [x] עדכון פונקציות ב-`messages.py` לתמיכה בתאריכים
- [x] עדכון ממשק ניהול החדשות ב-`messages_manager.py`
- [x] הוספת עמודת "תאריכי תזמון" לטבלה
- [x] תמיכה בפורמט DD.MM.YYYY בממשק
- [x] סינון אוטומטי של חדשות לפי תאריכים ב-`get_active_news_items()`

### רישיון חודשי (Monthly License)
- [x] הוספת שדה `expiry_date` למבנה הרישיון
- [x] יצירת פונקציה `_make_monthly_license_key()` עם סכמה SP3
- [x] יצירת פונקציה `validate_monthly_license_key()`
- [x] תמיכה ב-3 רמות: BASIC, EXTENDED, UNLIMITED
- [x] **עדכון ממשק הרישום ב-`admin_station.py`**

### ייצוא והצגה (סשן קודם) ✅
- [x] הוספת RTL וצבעים מתחלפים לכל ייצואי Excel
- [x] תיקון ייצוא רכישות מוצרים (מי קנה מה וכמה)
- [x] הוספת ייצוא תורנות אתגרים (לפי אתגר או כולם)
- [x] תיקון גדלי חלונות דיאלוג והפיכתם ל-resizable
- [x] הוספת אפשרות תצוגה מקדימה לייצואים

---

## סנכרון Web Admin – תיקונים קריטיים (מרץ 2026) 🔴

### עדיפות גבוהה
- [x] **תיקון סנכרון קניות:** Web לקרוא/לכתוב ישירות לטבלאות DB (products, product_categories) במקום web_settings
- [x] **תיקון סנכרון color_settings:** להוסיף ל-config-bridge ב-sync_agent (+ סנכרון color_settings.json)
- [x] **הוספת modals להודעות:** הוספה/עריכה ב-Web במקום alert/prompt (static, threshold, news, ads)
- [x] **דף מקסימום נקודות (תקרה):** שדרוג לתקרה דינמית מלאה (policy, weekday, special rules, free additions)
- [x] **הרחבת config-bridge:** quiet_mode, anti_spam כ-JSON blob settings + תיקון באג payload בסנכרון settings
- [x] **תיקון באג סנכרון settings:** payload ב-api_settings_save לא כלל key → הסנכרון המקומי לא יכל לעדכן settings

### עדיפות בינונית
- [ ] **וריאנטים למוצר:** הוספה לדף קניות ב-Web
- [ ] **ערכת צבעים מתקדמת:** הוספה לדף הגדרות תצוגה ב-Web
- [x] **ייצוא נוכחות/רכישות/פעולות מורה:** הוספה ל-Web (reports.py עם כל ה-APIs וה-exports)

### הושלם (מרץ 2026) ✅
- [x] תיקון תצוגת הודעות (רקע לבן, סדר כפתורים)
- [x] הסרת כפתורי קיצור מיותרים מהגדרות תצוגה
- [x] שכתוב דף שדרוגים ל-4 טאבים (צלילים+צבעים, מטבעות, יעדים, צלילי אירועים)
- [x] שכתוב דף קניות ל-3 טאבים (מוצרים, קטגוריות, הגדרות קופה)
- [x] דוח השוואה מקיף Web vs מקומי (COMPARISON_REPORT.md)
- [x] API endpoints: /api/products (CRUD), /api/product-categories (CRUD) ישירות ל-DB
- [x] סנכרון color_settings.json דו-כיווני (cloud ↔ local) ב-config-bridge
- [x] דיאלוגים מודאליים לכל סוגי ההודעות (הוספה + עריכה)
- [x] דף תקרת נקודות מלא: מדיניות, יומי/שבועי, לפי יום, כללים מיוחדים, תוספות חופשיות
- [x] הרחבת config-bridge ל-JSON blob settings (quiet_mode_config, anti_spam_config)

---

## מטלות ממתינות - עדיפות גבוהה 🔴

### בקשות חדשות מהמשתמש (פברואר 2026)
- [x] **ייצוא מוצרים:** ייצוא ברירת מחדל להורדות, שם ברירת מחדל "ייצוא קניות", כיוון טבלה מימין לשמאל
- [x] **הרחבת טבלת מוצרים:** הוספת עמודות מקושרות - כיתות מורשות (בהפרדת פסיק), סף נקודות, מקס' לתלמיד, מקס' לכיתה, מחיר מותנה מעל* נקודות>*% הנחה
- [x] **וריאציות מוצרים:** הוספת 5 עמודות ברירת מחדל של וריאציות בסוף טבלת המוצרים
- [x] **ייצוא/ייבוא:** 2 אפשרויות נפרדות - מוצרים / אתגרים
- [x] **ייבוא מוצרים/אתגרים:** שם הקטגוריה מתווסף אוטומטית לרשימת הקטגוריות
- [x] **שובר מרוכז:** שובר מרוכז לפריטים קטנים (כמו שביקשו קודם)
- [x] **מזעור חלון קריאת כרטיס מורה:** תיקון מזעור חלון פתיחה
- [x] **איחוד חלונות פתיחה:** mutex למניעת פתיחות כפולות + עיצוב חלון התחברות כמו splash
- [x] **מעברי עמוד בייצוא:** הוספת מעברי עמוד בכל סוגי הייצוא
- [x] **ניהול TODO.md:** עדכון ותחזוקה של TODO.md מכאן ואילך

### Web UI/SaaS – רשימת תיקונים מהבדיקה האחרונה (פברואר 2026)

- [x] עריכה ישירה בטבלת תלמידים: נקודות + הודעה פרטית (עדכון נקודות מהיר + עריכה מלאה במודל).
- [ ] סנכרון תמונת תלמיד וקישור לצפייה בפרטי התלמיד.
- [x] חלון עריכת מורה: כפתור סגירה (×) + כפתור ביטול.
- [x] עריכת פרטי מורים אחרים: תוקנה שגיאת RealDictCursor ב-Postgres (row[0] → row['class_name']).
- [ ] חלון כיתות: להסיר (לא קיים במקומי).
- [x] חלון ייבוא: כפתור חזרה לתפריט.
- [x] חלון ייצוא: תוקן /web/reports 404 – נוצר reports.py עם דף מלא + APIs + exports.
- [ ] עדכון מערכת: להבהיר/להסביר מה אופציות עדכון אוטומטי (יציב/בטא/פיתוח).
- [x] ניהול הודעות: CRUD מלא + show_always, start_date, end_date, sort_order.
- [x] חדשות (פס גלילה): CRUD מלא + הגדרות לוח יהודי וימי הולדת.
- [x] פרסומות: CRUD מלא.
- [x] בונוס זמנים: תמיכה בקבוצות + group_name, classes, days_of_week, sound_key, is_shown_public.
- [ ] בכל חלון: להוסיף כפתור חזרה.
- [ ] סנכרון לוגו + שם מוסד בכל הדפים (וגם מרכז ההדר).
- [x] סנכרון מלא של צבעים/צלילים/מטבעות/חגים/אנטי-ספאם מול התוכנה המקומית (כולם ב-color_settings key אחד).
- [x] הבהרה: אנטי-ספאם מול מצב שקט – הופרדו לדפים נפרדים עם הגדרות מלאות.
- [x] חלונות חסרים/שגויים: תוקנו קניות, מגבלת ניקוד, מצב שקט, ייצוא.
- [ ] עיצוב טבלאות: טקסט וכותרות צריכים להיות בהירים/קריאים; להבטיח ניגודיות בכל החלונות (כהה/בהיר).
- [ ] סדר כפתורים/חלונות בתפריט (כמו במקומי):
  - [ ] תלמידים
  - [ ] מורים
  - [ ] שדרוגים (צלילים וצבעים | מטבעות ויהלומים | יעדים | צלילי אירועים)
  - [ ] הודעות (הודעות כלליות | לפי ניקוד | חדשות | הודעות לבונוס זמנים | פרסומות)
  - [ ] בונוסים (בונוס מנהל | בונוס זמנים)
  - [ ] קניות
  - [ ] הגדרות תצוגה
  - [ ] הגדרות מערכת (הגדרות רישום | מצב שקט | מגבלת ניקוד | חגים וחופשות | אנטי ספאם)
  - [ ] ייבוא וייצוא
  - [ ] אזור אישי
- [ ] תפריט מורה: לראות רק תלמידים + בונוס מורה.

### חדש (ינואר 2026) – דרישות נוספות
- [x] **בדיקת קריסות מוסד ביתר:** ניתוח קובץ ה‑DB והפקת גורם אפשרי לקריסות/שיבוש נתונים
- [ ] **Local Sync (Single‑Writer):** שרת מקומי בעמדה ציבורית + קליינטים שמסתנכרנים במקום כתיבה ישירה
- [ ] **מדריך:** הסבר על `db_path`/`db_path.txt` (נתיב ידני) והשפעתו על כתיבה ישירה ל‑UNC בכל עמדה
- [x] **תיקון ידני לקובץ DB מושחת** – `C:\מיצד\SchoolPoints\נתונים ביתר\school_points.db`
- [x] **פריטיות מלאה לכל מסכי ניהול בווב** – תלמידים | ניהול מורים | דוחות | תצוגה ותוכן | הגדרות תצוגה (צבעים/צלילים/מטבעות) | הודעות | בונוס מיוחד | בונוס זמנים | בונוסים | חגים/חופשות | מערכת | הגדרות מערכת | לוגים | שדרוגים (שמות/פונקציונליות כמו במקומי)
  - [x] טבלת השוואה מלאה בין פעולות במקומי לבין ה‑Web (בדיקת חוסרים ותיקון)
- [x] **גרפיקה אחידה לכל המסכים** – הדר/פוטר, כפתורים בעיצוב אחיד עם אייקונים
  - [x] יצירת סט אייקונים לכל שורת פעולה/כפתור בממשק הווב
- [x] **אזור אישי מלא + שמירת התחברות** – הישארות מחובר עד יציאה, תפריט מוסד מלא
- [x] **סופר אדמין (ווב)** – ניהול כל המוסדות, עריכה מלאה (כולל שינוי Tenant ID), כניסה למוסד ללא סיסמה, רישום מוסדות
- [x] **תפריט תמחור (/web/pricing)** – טבלת השוואה בין מסלולים (Basic, Extended, Unlimited).
- [x] **תיקון “עדכון מהיר”** – מודאל נפתח במרכז בלי גלילה + התאמת צבעים/נראות טקסט
- [x] **מדריך/הורדה באתר** – החזרת דף מדריך/הורדה לפעולה תקינה
  - [x] העלאת `guide_user_embedded.html` העדכני מהתוכנה המקומית + קישור ל"ציוד נדרש"
  - [x] חיבור תמונות המדריך מתיקיית `תמונות/להוראות` והצגתן דרך /web/assets
- [x] **מחשב חדש: חיבור לענן + סנכרון מלא של הגדרות/נתונים מקומיים** – מימוש Snapshot Download ב-API וב-Agent.
- [x] **חיבור מחשב קיים למוסד חדש (Initial Push)** – כפתור "אתחול ענן" בעמדת הניהול לדחיפת כל המידע המקומי לענן.
- [x] **תמיכה ב-Tenant ID אלפא-נומרי** – הסרת מגבלת ספרות בלבד בסנכרון.
- [x] **עקיפת לוגים תקועים ב־DO** – להחזיר שגיאת traceback בדף /web/students באופן זמני כדי לקבל שגיאה גם בלי Runtime Logs
- [x] **דיאגנוסטיקה 500 ב־/web/students (לוג זמני לפרוד)** – להעלות לוג זמני לשרת כדי לקבל traceback
- [x] **תיקון ידני של זמן תיקוף/הגעה** – כפתור במסך ניהול תלמיד לבחירת תאריך+שעה, רישום כאילו תיקף בזמן הנבחר, כולל החלת בונוס זמנים וייצוא בהתאם.
- [x] **ניהול קופה: רענון מיידי אחרי שמירת מוצר/אתגר** – לאחר הוספה/עריכה, הרשימה תתעדכן מיידית בלי צורך לסגור/לפתוח את חלון הניהול.
- [x] **מסך קופה: גלילה עם אצבע/Touch** – גלילה לרשימת המוצרים ולעגלת הקניות גם ב-touch/trackpad (כיום רק גלגלת עכבר).
- [x] **רקע עמדה ציבורית: תיקון מונטאז' תמונות** – לתקן פריסת מונטאז' רקע כך שלא יהיו חללים שחורים גדולים בין תמונות.
- [x] **חשבונית/שובר: תאריך עברי** – להוסיף הדפסה של תאריך עברי בחשבונית ובשובר (כרגע לא מודפס).
- [x] **מערכת "מקסימום נקודות"** – הגדרות מערכת + חישוב תקרה נוכחית + אכיפה (אזהרה/חסימה) בעת הוספת נקודות.
- [x] **שדרוגים > יעדים: יעד לפי מקסימום נקודות** – יעד מוחלט דינמי שמבוסס על התקרה המחושבת.

### חדש (ינואר 2026) – דרישות לקוח נוספות
- [x] **הדפסה בעמדת קופה:**
  - [x] להחזיר לוגו בשוברים
  - [x] להדפיס שובר גם על מוצרים/וריאציות
  - [x] להסיר הדגשת תאריך עברי (רק להציג תאריך עברי)
  - [x] לתקן סוגריים הפוכים ב"משך (דקות)"
  - [x] להגדיל את שם המוצר/אתגר בשובר
- [x] **עמדת קופה:** החלקת כרטיס מאסטר 1 לפתיחת חלון "יציאה"/"הגדרות" + בחירת בסיס נתונים לשיוך העמדה
- [x] **עדכון גרסה:** לבדוק העתקת תיקיית sound למיקום המשותף בזמן עדכון
- [x] **חלון חסימות ספאם:** להוסיף כפתורי הוספת קודים ({מס' תיקופים}/{timeleft}) לטקסט
- [x] **שדרוגים > צבעים וצלילים:** תיקון שמירה של עריכת שורה קיימת ללא הוספה/מחיקה
- [x] **בחירת צליל:** להוסיף אפשרות "ללא צליל" בכל הרשימות (שדרוגים/אירועים/חסימות)
- [x] **פרסומות:** להסתיר פרסומת גם בעת בונוס מורה/בונוסים נוספים (לא רק תיקוף רגיל)
- [x] **פרסומות:** בזמן שיש חסימה (שבתות וחגים) גם הפרסומות לא יופיעו
- [x] **עדכונים אוטומטיים:** בבדיקת עדכון בהפעלה – להוריד אוטומטית גרסה חדשה אם קיימת, כולל תמיכה בקובץ manifest בגוגל דרייב
- [x] **גרסה בקובץ התקנה:** לכלול מספר גרסה בשם קובץ ההתקנה וגם בפרטי "תוכניות ותכונות" בוינדוס
- [ ] **שדרוג מדורג למצב מקוון/היברידי:** תכנון מסלול שדרוג כך שמי שלא מפעיל מוד מקוון ימשיך לעבוד ללא שינוי
- [x] **אנטי‑ספאם (עמדה ציבורית):** תלמיד חסום לא אמור לראות את הודעת התלמיד הקודם; לנקות את הודעת הסטטוס בעת חסימה

### מצב מקוון/היברידי (Hybrid / Cloud) – קריטי
- [ ] **דיבאג 'מורה לא נמצא' אחרי חיבור לענן:** בדיקת ספירת מורים מקומי/ענן, ואימות Snapshot/Pull/Push בהתאם.
- [x] **ענן: endpoint Snapshot** – הוספת `/sync/snapshot` להעלאת teachers+students ל-tenant DB בענן (bootstrap)
- [x] **מקומי: snapshot upload** – הרחבת `sync_agent.py` עם `--snapshot` לשליחת teachers+students לענן
- [x] **DB Persistency** – מעבר למסד נתונים מנוהל (Postgres) כדי שלא יאבד מידע ב-redeploy
- [x] **Postgres: Multi-tenant** – נתוני tenant נשמרים ב-Postgres (schema לכל tenant) במקום קבצי `.db`
- [x] **Debug Sync Agent** – הוספת מצבי דיבאג להצגת change_log (`--show-pending`, `--show-all-changes`)
- [x] **בדיקת קבלה (Acceptance)** – snapshot OK ואז `--once` משנה נקודות ונראה באתר, ואז redeploy והנתונים נשמרים
- [x] **סנכרון קבצים (Images/Sounds)** – מימוש `/sync/files` API בשרת ואינטגרציה ב-`sync_agent.py` (רץ כל 5 דקות).
- [x] **תיקון cursor אחרי Snapshot** – `last_event_id` לפי `sync_events.id` + עדכון הלקוח לשמירת `pull_since_id` כדי לעבור לדלתאות ולא למשוך שוב היסטוריה.
- [x] **תיקון sync_agent** – תיקון לולאת הסנכרון (שגיאות אינדנטציה/משתנים) + תיקון קריאה ל-file-sync.
- [ ] **בהתחברות ראשונה:** לשאול "לעדכן נתונים בענן?" + בדיקת סטטוס מי יותר מעודכן (local vs cloud)
- [ ] **הקשחת SQLite מקומי (UNC):** PRAGMA בטוחים, busy_timeout, retry, גיבויים אוטומטיים, ושחזור במקרה של DB פגום
- [x] **סנכרון רציף:** לאחר snapshot – סנכרון פעולות באופן קבוע (לא "מצב")
  - [x] Pull/Pull-cursor לפי `sync_events.id` + החלת פעולות נקודות כ-delta (מצטבר)
  - [x] החלת פעולות בסיסית: נקודות + settings + הודעות
  - [x] הרחבת החלת פעולות לכל שאר הישויות/טבלאות (תלמידים מלא, מורים, כיתות, מוצרים)
- [ ] **אידמפוטנטיות:** מנגנון `op_id`/`event_id` כך שפעולה שנשלחה פעמיים לא תכפיל נקודות
- [ ] **התנגשויות:** מסך השוואה + יומן פעולות + בחירה מה לשמור כשיש חוסר התאמה
- [ ] **Deploy לענן:** commit+push + redeploy ב-DigitalOcean כדי שכל זה יעבוד בפרודקשן
- [x] **Snapshot2 מלא בענן** – endpoints `/sync/snapshot2` (GET/POST) לסנכרון מלא דו־כיווני
- [ ] **תיקון Bootstrap מורה** – למנוע מסך "הגדרת מנהל" כשיש מורים קיימים ולתקן שגיאת 500 ביצירת מורה
  - [x] תיקון 500 ב-Postgres בעת יצירת מורה (קריאה ל-row dict ב-SELECT MAX(id))
  - [x] הוספת מסך בחירה: חיבור עמדה (Pairing) או יצירת מנהל ראשוני, והפניה לשם במקום bootstrap ישיר
- [x] **יישור סדר אייקונים בווב** – סדר וקטגוריות במסך /web/admin כמו בעמדת הניהול המקומית
- [ ] **שיפור תהליך חיבור תוכנה לאתר (פברואר 2026):**
  - [ ] חיוב הזנה מחדש של קוד מוסד וסיסמה בכל חיבור (גם אם כבר מחובר)
  - [ ] אחרי אישור - סנכרון אוטומטי של פרטי מורים עם הודעת התקדמות
  - [ ] הזנת סיסמת מורה אחרי סנכרון המורים
  - [ ] סנכרון מלא בסדר עדיפויות: תלמידים → הגדרות → קבצים נלווים
  - [ ] הצגת אחוז התקדמות בזמן אמת
  - [ ] סנכרון לוגו מוסד להדר האתר עם שם המבצע

#### Hybrid UX (ללא פקודות ידניות)
- [x] **אוטומציה ברמת לקוח (בשלב ראשון)** – עמדת ניהול מפעילה `sync_agent` בלולאה ברקע כשמצב הוא hybrid/cloud ויש פרטי סנכרון
- [ ] **אוטומציה מלאה** – Windows Task Scheduler / שירות לכל העמדות (כולל public/cashier)
- [ ] **ריצת רקע בטוחה** – backoff בשגיאות רשת + הגבלת תדירות + לוג קצר
- [ ] **חיבור אוטומטי לסנכרון מתוך התוכנה** – הפעלה/עצירה מתוך GUI לפי מצב (local/hybrid/cloud)

### תכנון עתידי (Future Roadmap)
- [ ] **בחירת מצב מערכת (System Mode):**
  - [ ] **מצב רגיל (ברירת מחדל):** צבירת נקודות לפי בונוסים/הזנת מורה (המצב הנוכחי).
  - [ ] **מצב צבירת שעות:** שעון נוכחות – תלמיד מתקף כניסה ויציאה, והמערכת מחשבת ומזכה בשעות שנצברו.
  - [ ] **מצב נקודות על תיקוף:** זיכוי קבוע בנקודות על כל תיקוף (הגדרת שעות פעילות, כמות נקודות לתיקוף, מגבלת פעמים/כפל תיקוף ביום).

### Web UI (אתר) – חובה
- [x] **הדר אחיד בכל הדפים** – הדר דומה לדף הבית עם כפתור "בית"/"אחורה" בכל דף (כולל מדריך וצור קשר)
- [x] **אזור אישי בהדר** – לפני התחברות: "התחברות"; אחרי התחברות: שם/קוד מוסד + כניסה לתפריט מוסד
  - [ ] החלפת סיסמה
  - [ ] שכחתי סיסמה
  - [ ] פרטי רישום ותשלומים
  - [ ] פרטים נוספים
- [x] **מדריך: תיקוני RTL/תצוגה** – להסיר עיגולים/אלמנטים מיותרים ולוודא תמונות תקינות
- [x] **מדריך: לוודא גרסה עדכנית** – להשוות ל-`C:\ProgramData\SchoolPoints\guide_user_embedded.html` ולהעלות לגרסת האתר
- [x] **מדריך: תמונות לא נטענות** – למפות src של התמונות ב-HTML ולוודא הגשה נכונה בשרת (`/web/assets/...` או rewrite)
- [x] **צור קשר: טופס אמיתי** – שם, מייל, נושא, הודעה, שליחה ל-`info.schoolpoints@gmail.com` באמצעות SMTP.
- [x] **תפריט תמחור** – דף Pricing מלא (/web/pricing) עם חבילות.
- [ ] **רישום מקוון + סליקה** – תשתית לרישום מוסד/רישוי מקוון, תשלום בכרטיס אשראי, התאמת מחולל רשיונות
- [ ] **מחיקת מוסד מלאה** – מחיקת רשומות מוסד + מחיקת DB של המוסד + מחיקת קבצים ייחודיים למוסד (כדי לא לתפוס מקום)
- [ ] **אופטימיזציית אחסון (Sound)** – תיקיית סאונד משותפת לקבצי ברירת מחדל (למניעת כפילות) ורק קבצים אישיים בתיקיית הטננט

### Web Admin (עמדת ניהול ווב)
- [x] **טבלת תלמידים מלאה** – עמודות זהות לממשק המקומי: מס' סידורי, משפחה, פרטי, כיתה, נקודות, הודעה פרטית, וכל שאר הפרטים
  - [x] בחירת שורה
  - [x] עריכה ישירה במקום (כולל חסימת תיקון חופשי)
  - [x] לא לחסר מאומה ביחס לממשק המקומי

#### מורים (Web)
- [x] **מסך ניהול מורים** – רשימה + חיפוש + CRUD בסיסי + API (`/web/teachers`, `/api/teachers*`)
- [x] **יישור קו מלא לממשק המקומי** – הרשאות/כיתות למורה/הגבלות בונוסים/שדות נוספים

#### מסכים נדרשים (אין עדיפות – כולם קריטיים)
- [x] **שדרוגים** – מלא
- [x] **הודעות** – מלא
  - [x] יצירת טבלאות הודעות ב-tenant DB (static/threshold/news/ads/student)
  - [x] API מלא לניהול הודעות + פרסומות (CRUD/תזמון/סידור/העלאת תמונה)
  - [x] UI מלא למסך הודעות בווב (טאבים, טבלאות, פעולות כמו במקומי)
- [x] **בונוסים** – מלא
- [x] **בונוס מיוחד** – מלא
- [x] **בונוס זמנים** – מלא
- [x] **חגים/חופשות** – מלא
- [x] **הגדרות מערכת** – מלא
- [x] **הגדרות תצוגה** – מלא
- [x] **קופה** – מלא
- [x] **דוחות** – מלא
- [x] **לוגים** – מלא

#### Pairing / חיבור אוטומטי (כמו OAuth Device Code)
- [x] **יצירת מוסד באתר** – הזנת נתונים + יצירת tenant_id + password + api_key
- [x] **קוד התאמה (device code)** – השרת מנפיק `device_code/user_code`
- [x] **מסך אישור באתר** – הזנת `user_code` ואישור למכשיר
- [x] **Poll מהתוכנה** – התוכנה מקבלת `tenant_id/api_key/sync_url` אחרי אישור
- [x] **כתיבה אוטומטית ל-config.json** – עדכון פרטי חיבור בלי הקלדה ידנית
- [ ] **הסתרת פרטי חיבור בממשק המקומי** – אין שדות API Key/URL, רק כפתור התחבר/התנתק

#### Onboarding לענן בלבד
- [ ] **מורה ראשון** – יצירת קוד/סיסמת מורה ראשונית בעת פתיחת מוסד חדש בענן (כדי לאפשר כניסה ראשונה)

### 1. השלמת רישיון חודשי
**קבצים:** `admin_station.py`, `license_manager.py`

- [x] עדכון דיאלוג רישום רישיון:
  - [x] הוספת רדיו בטון "רישיון חודשי"
  - [x] שדה בחירת תאריך תפוגה (DatePicker או Entry)
  - [x] כפתור "צור קוד רישיון חודשי"
  - [x] הצגת הקוד שנוצר
  
- [x] בדיקת תוקף רישיון חודשי:
  - [x] בדיקה בהפעלת המערכת
  - [x] השוואת `expiry_date` לתאריך נוכחי
  - [x] הצגת אזהרה 7 ימים לפני תפוגה
  - [x] חסימת גישה לאחר תפוגה + חלון הזנת רישיון חדש
  
- [x] עדכון `LicenseManager`:
  - [x] הוספת `is_monthly` property
  - [x] הוספת `days_until_expiry()` method
  - [x] הוספת `is_expired()` method לרישיון חודשי
  - [x] עדכון `can_run_*()` methods לבדוק תוקף חודשי

### 2. חסימת תיקופים לחינם
**קבצים:** `database.py`, `admin_station.py`, `public_station.py`

- [x] הוספת שדה `is_free_fix_blocked` לטבלת students
- [x] הוספת checkbox בממשק הניהול לחסימת תיקופים חינם לתלמיד
- [x] בדיקה בעמדה הציבורית - אם חסום, לא לאפשר תיקוף חינם
- [x] הצגת הודעה מתאימה לתלמיד שחסום
- [x] אפשרות לביטול חסימה ע"י מנהל

### 3. אייקונים ועיצוב
**קבצים:** קבצי `.ico`, `.spec`, `.iss`

- [x] עיצוב סט אייקונים מותאם:
  - [x] אייקון עמדת ניהול (Admin) - כוכב/מנהל
  - [x] אייקון עמדה ציבורית (Public) - תלמיד/משתמש
  - [x] אייקון עמדת קופה (Cashier) - עגלת קניות/מטבע
  - [x] אייקון מתקין (Installer) - לוגו ראשי
  
- [x] יצירת קבצי `.ico` בגדלים שונים:
  - [x] 16x16, 32x32, 48x48, 64x64, 128x128, 256x256
  
- [x] עדכון קבצי `.spec`:
  - [x] `SchoolPoints_Admin.spec` - icon='icons/admin.ico'
  - [x] `SchoolPoints_Public.spec` - icon='icons/public.ico'
  - [x] `SchoolPoints_Cashier.spec` - icon='icons/cashier.ico'
  
- [x] עדכון `SchoolPoints_Installer.iss`:
  - [x] SetupIconFile - אייקון המתקין
  - [x] UninstallDisplayIcon - אייקון הסרת התקנה
  - [x] קיצורי דרך לשולחן העבודה עם אייקונים
  - [x] קיצורי דרך לתפריט התחל

### 3. תיקון build_full.bat
- [x] שיפור טיפול בשגיאות pip install
- [x] הוספת timeout ארוך יותר (60-120 שניות)
- [x] הסרת --only-binary שגורם לבעיות
- [x] הוספת ניסיון חוזר אוטומטי
- [x] הוספת הדפסות ברורות יותר

---

## נושאים פתוחים מסשן 19.4.2026 🔴

### באג קריטי: עמדה נתקעת ב-15% בהפעלה ראשונה (מחשב חדש)
- **תסמין:** אחרי סיום ההתקנה, בהפעלה ראשונה של העמדה הציבורית התוכנה נתקעת ב-15% ("מאתחל סנכרון...") ולא מתקדמת גם לאחר שעה.
- **שורש הבאג:** `public_station.py` שורה 601 — `_maybe_start_sync_agent()` קוראת ל-`load_app_config()` שמנסה `os.path.isdir("\\OLDCITY\SchoolPoints")` — קריאה חוסמת ללא הגבלת זמן כשהרשת לא זמינה (מחשב חדש, לא מחובר לרשת).
- **תיקון חלקי שבוצע:** הוחלף `os.path.isdir` ב-thread+3s timeout ב-`load_app_config` של `public_station.py` ו-`admin_station.py` — אך ייתכן שיש קריאות נוספות חוסמות ב-`_maybe_start_sync_agent` עצמה.
- [ ] **לבדוק:** האם `_maybe_start_sync_agent` עצמה חוסמת (לא רק `load_app_config`) — לעטוף בthread עם timeout או להפוך לאסינכרוני מלא.
- [ ] **לבדוק:** כל קריאות `os.path.isdir` / `os.path.exists` על UNC בכל קוד האתחול (שורות 570-650 ב-`public_station.py`).
- [ ] **לבדוק:** `ensure_shared_folder_config()` — אם גם היא חוסמת בגישה לUNC.
- [ ] **לאחר תיקון:** לקמפל ולבדוק על מחשב חדש ללא רשת.

### פיצ'ר: בחירת התקנה מקוונת/מקומית בהתקנה ראשונית
- [ ] בהפעלה ראשונה (wizard): שאל "התקנה מקומית" או "חיבור לחשבון קיים (מקוון/היברידי)".
- [ ] אם "מקוון": חיבור לחשבון קיים + סנכרון נתונים אוטומטי (כבר קיים ב-Pairing).
- [ ] אם "מקומי": זרימה רגילה כמו היום.
- [ ] לממש בכל 3 העמדות (admin/public/cashier).

### אופטימיזציה: קבצים כפולים בחבילת ההתקנה
- **בעיה:** dist תופס 450MB — 3 תיקיות כל אחת עם Python runtime מלא + DLLs כפולות + קבצי סאונד כפולים.
- [ ] קבצי סאונד (.wav) — 8MB כפול בPublic + Cashier — לא נחוצים בקופה כלל. להסיר מ-Cashier.
- [ ] DLLs משותפות — ליצור תיקיית `_shared` ולהפנות מ-.iss עם `ExternalDependenciesDir`.
- [ ] לשנות `SchoolPoints_Installer.iss`: `SolidCompression=no` כדי להאיץ את תהליך ההתקנה (15% נראה תקוע).
- [ ] לשקול PyInstaller onedir עם shared `_MEIPASS` או תיקיית lib משותפת.

### PayMe — ממתין לתשובת שירות לקוחות
- [ ] לקבל מ-PayMe ישראל: מהו ה-seller_payme_id הנכון לסביבת preprod (sandbox).
- [ ] לקבל מ-PayMe: URL ישיר לפרודקשן (לאחר אישור KYC).
- [ ] עד אז: `PAYME_TEST_MODE=1` + `PAYME_SELLER_ID=NPL...` ב-DigitalOcean.

---

## מטלות עתידיות - עדיפות בינונית 🟡

### שיפורי מערכת רישוי
- [ ] מחולל קודי רישיון חודשי אוטומטי (כלי למנהל)
- [ ] מעקב אחר היסטוריית רישיונות
- [ ] התראות מייל לפני תפוגת רישיון
- [ ] פאנל ניהול רישיונות מרכזי

### שיפורי ייצוא ודוחות
- [ ] ייצוא לפורמטים נוספים (PDF, CSV)
- [ ] תבניות דוחות מותאמות אישית
- [ ] גרפים וויזואליזציות
- [ ] שליחת דוחות במייל אוטומטית

### שיפורי ממשק משתמש
- [ ] ערכת נושא כהה (Dark Mode)
- [ ] גדלי גופן מותאמים אישית
- [ ] קיצורי מקלדת נוספים
- [ ] סרגל כלים מהיר

---

## רעיונות לטווח ארוך 💡

### אינטגרציות
- [ ] סנכרון עם Google Sheets
- [ ] אינטגרציה עם מערכת הודעות SMS
- [ ] חיבור למערכת ניהול בית ספר קיימת
- [ ] API לגישה חיצונית

### פיצ'רים מתקדמים
- [ ] גיבוי אוטומטי לענן
- [ ] אפליקציית מובייל (תצוגה בלבד)
- [ ] מצב רב-משתמשים עם הרשאות
- [ ] מערכת התראות מתקדמת
- [ ] דשבורד אנליטיקס

---

## הערות טכניות 📝

### קבצים עיקריים במערכת
```
SchoolPoints/
├── admin_station.py          # עמדת ניהול ראשית
├── public_station.py          # עמדה ציבורית לתלמידים
├── cashier_station.py         # עמדת קופה
├── database.py                # ניהול מסד נתונים
├── license_manager.py         # מערכת רישוי
├── messages.py                # מערכת הודעות וחדשות
├── messages_manager.py        # ממשק ניהול הודעות
├── excel_import.py            # ייבוא/ייצוא Excel
├── excel_styling.py           # עיצוב קבצי Excel (RTL, צבעים)
├── jewish_calendar.py         # לוח עברי
└── TODO.md                    # קובץ זה
```

### פורמטים וסטנדרטים
- **תאריכים במסד נתונים:** `YYYY-MM-DD` (ISO 8601)
- **תאריכים בממשק משתמש:** `DD.MM.YYYY` (פורמט ישראלי)
- **קודי רישיון:** `XXXXX-XXXXX-XXXXX-XXXXX` (20 תווים, 4 קבוצות)
- **סכמות רישיון:**
  - SP1: רישיון ישן (ללא קוד מערכת)
  - SP2: רישיון חדש (עם קוד מערכת)
  - SP3: רישיון חודשי (עם תאריך תפוגה)

### רמות רישיון
- **Trial:** 7 ימי ניסיון, 2 תחנות
- **Basic:** 2 תחנות
- **Extended:** 5 תחנות
- **Unlimited:** 999 תחנות
- **Monthly:** כל הרמות לעיל, עם תאריך תפוגה

### משתני סביבה חשובים
- `TRIAL_DAYS = 7` - מספר ימי ניסיון
- `APP_VERSION = "1.6.1"` - גרסת האפליקציה
- `ENABLE_PURCHASES` - דגל להפעלת מערכת הקופה

---

## תהליך עבודה מומלץ 

1. **לפני תחילת עבודה:**
   - עדכן קובץ זה עם המטלה החדשה
   - סמן מטלות קיימות כ-"בתהליך"
   

2. **במהלך העבודה:**
   - עדכן סטטוס מטלות שהושלמו
   - הוסף הערות טכניות רלוונטיות
   - תעד שינויים משמעותיים

3. **בסיום עבודה:**
   - סמן מטלות שהושלמו ✅
   - הוסף תאריך עדכון
   - בדוק אם יש מטלות נגררות

---

**עדכון אחרון:** 23/03/2026 (תיקון כפילויות בייצוא + תיקון דיאלוג הגדרה נעלם)  
**מטלה נוכחית:** תיקוני באגים קריטיים במנגנון ארכיון DB ובתיקון תיקוף מרוכז  
**הבא בתור:** וריאנטים למוצר בווב, תפריט מורה מוגבל, ערכת צבעים מתקדמת, Local Sync single-writer

### תיקוני ארכיון + ייצוא בונוס + תיקון תיקוף מרוכז (מרץ 2026 – סיבוב 2):
- [x] **באג: פונקציות בדיקת בונוס לא בודקות ארכיון:** `get_student_time_bonus_for_group_on_date`, `has_student_received_time_bonus_group_on_date`, `has_student_received_time_bonus_on_date` בדקו רק main DB. כשנתונים הועברו לארכיון → תיקון ידני לא זיהה בונוס קיים → כפל נקודות אפשרי / רשומות חסרות. תוקן: fallback לארכיון בכל 3 הפונקציות (database.py).
- [x] **באג UTC בתיקון ידני:** `datetime.now().astimezone().tzinfo` נותן timezone של **היום** ולא של התאריך היעד → הפרש שעה בתקופת שינוי שעון קיץ. תוקן: שימוש ב-`time.mktime(dt_obj.timetuple())` שמכבד DST של התאריך עצמו (admin_station.py, שני המקומות).
- [x] **באג: except:pass בולע שגיאות בתיקון מרוכז:** כל שלב ב-`_bulk_fix_apply_single` בלע שגיאות בשקט → תיקוף נרשם אבל בונוס לא. תוקן: logging עם `safe_print(f"[BULK-FIX] ...")` בכל נקודת כשל.
- [x] **באג dedup בייצוא:** `get_time_bonus_given_for_date/bonus` השתמשו ב-ID למניעת כפילויות, אבל כשתלמיד יש לו רשומה ב-main + archive עם IDs שונים → כפילות בייצוא. תוקן: dedup לפי `(student_id, bonus_schedule_id, given_date)` במקום ID.
- [x] **logging ל-_get_archive_connection:** הוספת הודעות שגיאה כשארכיון לא נמצא / לא נגיש / חסר טבלאות.
- [x] **תיקון DB רובע 12.3:** סקריפט `fix_march12_bonuses.py` — הורץ על DB של LOG, 31 רשומות time_bonus_given הושלמו (0 נק', נוכחות לייצוא). DB מוכן להשתלה.
- [x] **באג: ענף "אותן נקודות" בתיקון ידני/מרוכז:** כשרשומת בונוס קיימת **רק בארכיון**, `update_time_bonus_given_at_for_group_on_date` עושה UPDATE על main DB (0 rows) → לא נוצרת רשומה. תוקן: שימוש ב-`record_time_bonus_given_on_date` (UPSERT) שיוצר רשומה חדשה אם לא קיימת. תוקן ב-`_bulk_fix_apply_single` + `open_manual_swipe_override` (admin_station.py).
- [x] **logging ב-`open_manual_swipe_override`:** הוחלפו `except Exception: pass` ב-logging עם prefix `[MANUAL-FIX]`.
- [x] **באג כפילויות בייצוא היסטוריית נקודות לתלמיד:** `get_points_log_for_student` מיזג `points_log` (main+archive) עם `points_history` (main+archive), אבל ה-signature dedup (`sig_points_log`) נבנה רק מ-main `points_log` → רשומות שארכבו ל-archive `points_log` לא סוננו מ-`points_history` → כל שורה הופיעה פעמיים (אחת עם old_points, אחת בלי). תוקן: dedup סופי ב-merge step שבונה signature set מכל ה-`points_log` (main + archive) ומסנן `points_history` כפולים (database.py).
- [x] **באג: דיאלוג הגדרה ראשונית נעלם (התקנה נתקעת ב-15%):** `_open_admin_shared_folder_dialog` ו-`_open_initial_admin_dialog` השתמשו ב-`transient(self.root)` כש-root מוסתר (withdrawn). ב-Windows, חלון transient לחלון מוסתר בלתי נראה → המשתמש רואה loading ב-15% בזמן שדיאלוג ההגדרה חופשי פתוח מאחוריו. תוקן: הסרת `transient`, הוספת `lift()+focus_force()`, והסתרת חלון הטעינה לפני פתיחת דיאלוגים (admin_station.py).
- [ ] **לוודא שתיקוני תיקוף מרוכז שהמשתמש עשה מופיעים בייצוא** — אחרי השתלת DB + קוד מעודכן, לבדוק.

### תיקוני ארכיון וניקוי DB (מרץ 2026) – אובדן נתוני הגעה:
- [x] **באג UTC קריטי:** `datetime('now', '-7 days')` מחזיר UTC אבל timestamps נשמרים בשעון מקומי → מחיקה ~2 שעות מוקדם מדי. תוקן: הוספת `'localtime'` לכל שאילתות datetime בניקוי+ארכיון.
- [x] **באג: עמדה משנית מוחקת מ-shared DB ללא ארכיון:** בסביבת shared folder כל העמדות כותבות לאותו DB פיזי. אם secondary מחקה טבלאות דיווח לפני שה-master ארכב — הנתונים אובדים לצמיתות. תוקן: secondary מדלגת על מחיקת טבלאות דיווח כש-db_path הוא UNC (= DB משותף); מוחקת רק מ-DB מקומי (sync client).
- [x] **חלון שמירה קצר מדי:** 7 ימים צפוף לבית ספר. הוגדל ל-14 ימים בארכיון master ובמחיקת secondary.

### ביקורת סנכרון מקיפה (מרץ 2026) – ממצאים ותיקונים:
- [x] **BUG קריטי: entity_type מרובים** – הווב שלח `students`/`teachers`/`teacher_classes` (רבים) אבל sync_agent מצפה ליחיד (`student`/`teacher`/`teacher_class`). תוקן ב-students.py + teachers.py.
- [x] **BUG קריטי: student_points payload** – הווב שלח `points`+`delta` אבל sync_logic מצפה ל-`old_points`+`new_points`. תוקן ב-students.py.
- [x] **BUG: teacher_class/replace לא מטופל** – הווב שלח action_type='replace' שלא היה handler עבורו. הוספנו handler ב-sync_agent.py + sync_logic.py.
- [x] **חוסר: ייבוא תלמידים ללא sync events** – ייבוא Excel לא שלח sync events ליצירת/עדכון תלמידים. תוקן ב-import_export.py.
- [x] **חוסר: public_closures ללא sync** – שמירה/מחיקה ב-web_settings בלבד ללא settings table ו-sync event. תוקן ב-settings.py.
- [x] **חוסר: max_points_config ללא sync** – שמירה ב-web_settings בלבד. תוקן ב-settings.py.
- [x] **Postgres RealDictCursor** – תיקון row[0] → dict access ב-teachers.py, reports.py, teacher_auth.py.
- [x] **UI: כפתור סגירה למודל עריכת מורה + כפתור חזרה בדף ייבוא**

### מצב סנכרון אחרי הביקורת:
| entity_type | Web→Cloud | Cloud→Local | Local→Cloud | הערות |
|---|---|---|---|---|
| student | ✅ | ✅ | ✅ | CRUD + import |
| student_points | ✅ | ✅ | ✅ | quick-points + import |
| teacher | ✅ | ✅ | ✅ | CRUD |
| teacher_class | ✅ | ✅ | ✅ | replace action |
| setting | ✅ | ✅ | ✅ | כל ההגדרות |
| time_bonus | ✅ | ✅ | ✅ | CRUD + groups |
| product | ✅ | ✅ | ✅ | CRUD |
| product_category | ✅ | ✅ | ✅ | CRUD |
| static_message | ✅ | ✅ | ✅ | |
| threshold_message | ✅ | ✅ | ✅ | |
| news_item | ✅ | ✅ | ✅ | |
| ads_item | ✅ | ✅ | ✅ | |
